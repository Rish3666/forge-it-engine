// DistroForge — Landing Page
// Composes the HeroSection with the landing page content.
// This is a server component; all interactivity is in the client components.

import HeroSection from "@/components/HeroSection";

export default function HomePage() {
  return (
    <main>
      <HeroSection />
    </main>
  );
}
